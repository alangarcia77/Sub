<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.subasta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".recuperar"></activity>
        <activity
            android:name=".ui.login.recuperar"
            android:label="@string/title_activity_recuperar" />
        <activity android:name=".Reloj" />
        <activity android:name=".libro" />
        <activity android:name=".arte" />
        <activity android:name=".automoviles" />
        <activity android:name=".PC" />
        <activity android:name=".smartphone" />
        <activity android:name=".principal2" />
        <activity android:name=".Registro2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>